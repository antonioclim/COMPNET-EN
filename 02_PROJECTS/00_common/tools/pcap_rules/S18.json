{
  "project": "S18",
  "pcap_relpath": "artifacts/pcap/traffic_e2.pcap",
  "decode_as": [],
  "rules": [
    {
      "id": "R1",
      "filter": "tcp.dstport==5018 && tcp.flags.syn==1 && tcp.flags.ack==0",
      "condition": ">= 1",
      "description": "Handshake to the reservation service."
    },
    {
      "id": "R2",
      "filter": "tcp.port==5018 && frame contains \"HOLD\"",
      "condition": ">= 1",
      "description": "At least one hold request appears in payload."
    },
    {
      "id": "R3",
      "filter": "tcp.port==5018 && (frame contains \"BUSY\" || frame contains \"OVERLAP\" || frame contains \"REJECT\")",
      "condition": ">= 1",
      "description": "Overlapping request is refused deterministically."
    },
    {
      "id": "R4",
      "filter": "tcp.port==5018 && frame contains \"CONFIRM\"",
      "condition": ">= 1",
      "description": "Promotion from hold to reservation is visible."
    },
    {
      "id": "R5",
      "filter": "tcp.port==5018 && (frame contains \"EXPIRED\" || frame contains \"RELEASED\" || frame contains \"CANCELLED\")",
      "condition": ">= 1",
      "description": "Hold release semantics are observable."
    }
  ]
}
