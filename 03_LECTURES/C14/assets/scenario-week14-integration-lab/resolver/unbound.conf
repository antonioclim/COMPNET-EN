server:
  interface: 0.0.0.0
  access-control: 0.0.0.0/0 allow
  verbosity: 1

  # These limits are intentionally small so caching effects are easier to observe.
  cache-max-ttl: 60
  cache-min-ttl: 0

forward-zone:
  name: "week14.local."
  forward-addr: 10.14.0.10@53
