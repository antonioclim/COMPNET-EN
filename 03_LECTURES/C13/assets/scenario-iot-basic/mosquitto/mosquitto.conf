listener 1883
allow_anonymous true
persistence false

# Optional extra configuration snippets can be mounted under:
#   /mosquitto/config/conf.d/*.conf
# This allows "compose override" files to enable features (e.g., TLS)
# without changing the default plaintext behaviour.
include_dir /mosquitto/config/conf.d
