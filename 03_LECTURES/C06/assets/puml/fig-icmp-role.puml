@startuml
skinparam backgroundColor white
skinparam shadowing false
skinparam roundcorner 12
skinparam defaultFontName Arial
skinparam defaultFontSize 14

title ICMP: role (diagnostics + errors)

rectangle "IP (IPv4)" as IP
rectangle "ICMP\n(errors + control)" as ICMP
rectangle "Tools\nping, traceroute" as TOOLS
rectangle "Typical messages:\n- Echo req/rep\n- Destination Unreachable\n- Time Exceeded" as MSG

ICMP --> IP : encapsulated in IP
TOOLS --> ICMP : use ICMP
MSG --> ICMP
@enduml
